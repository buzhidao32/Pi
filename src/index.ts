// ============================================================
// index.ts — 入口文件（最精简）
// 职责只有一行：import 启动函数并调用。
// 所有逻辑都在 cli.ts → agent.ts → llm.ts / tools/ 里。
// ============================================================

import { main } from "./cli";

main();
